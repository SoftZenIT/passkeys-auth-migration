{
  "skill_name": "passkeys-auth-migration",
  "evals": [
    {
      "id": 1,
      "prompt": "I have a NestJS app using Prisma with PostgreSQL and JWT auth. I want to add passkey support so users can sign in with their fingerprint instead of a password.",
      "expected_output": "A backend migration plan covering: @simplewebauthn/server install, Prisma schema addition (passkeys table with credentialId, publicKey, counter, aaguid, passkeyUserId on User), 6 endpoints with correct auth guards, RP config from env vars, Redis challenge storage. Followed by a frontend plan with conditional UI setup.",
      "should_trigger": true,
      "assertions": [
        "Plan is presented before any code is written",
        "No implementation code shown before plan approval",
        "Correct library selected: @simplewebauthn/server for NestJS",
        "Prisma schema includes credentialId as Bytes @unique",
        "Prisma schema includes counter as BigInt (not Int)",
        "Prisma schema includes aaguid field (String)",
        "User model gets passkeyUserId field (String, PII-free)",
        "6 endpoints listed: register-challenge, register-verify, auth-challenge, auth-verify, list, delete",
        "Register endpoints require @UseGuards(JwtAuthGuard) or equivalent",
        "Auth endpoints are public (no guard required)",
        "rpId configured from RP_ID environment variable (domain-only, no protocol)",
        "Number(passkey.counter) conversion shown in service layer when reading from database",
        "BigInt(newCounter) conversion shown when writing counter updates after verification",
        "Challenge deletion in both success and catch blocks (replay protection)",
        "Backward compatibility: existing JWT login preserved",
        "Unguarded registration endpoint would fail with 401 (auth boundary check)",
        "v1.1.0: NestJS DTO accepts SimpleWebAuthn v13+ extra fields (publicKeyAlgorithm, publicKey, authenticatorData) — does NOT use forbidNonWhitelisted: true that would strip them",
        "v1.1.0: Prisma BigInt counter is converted with Number() before any JSON.stringify or response serialization",
        "v1.1.0: Plan mode is activated before code (EnterPlanMode in Claude Code, or output complete plan + 'Waiting for approval')",
        "v1.1.0: Phase 0 scans for design system (tailwindcss, @mui/*, @shadcn/ui, chakra-ui, antd, mantine, etc.)",
        "v1.1.0: Phase 0 scans for i18n libraries (i18next, react-i18next, next-intl, react-intl, etc.)",
        "v1.1.0: Implementation Completeness Checklist presented at end of Phase 3",
        "v1.1.0: PATCH /auth/passkey/:id endpoint included for renaming passkeys",
        "v1.1.0: Default passkey name resolved from AAGUID at registration time",
        "v1.1.0: Install verification step (npm install / package check) runs before backend curl checkpoint",
        "v1.2.0: @simplewebauthn/server installed at v13 (not v11); no separate @simplewebauthn/types dependency (retired in v13 — types ship inside server/browser packages)"
      ]
    },
    {
      "id": 2,
      "prompt": "my django app uses postgres and argon2 for passwords. can you add passkey login?",
      "expected_output": "Backend plan using py_webauthn, Django model with aaguid and passkey_user_id migration, 4 view functions (register challenge/verify, auth challenge/verify), session-based challenge storage, plus list/delete views.",
      "should_trigger": true,
      "assertions": [
        "Plan presented before code",
        "Library: py_webauthn selected for Django",
        "Django model includes aaguid field (CharField)",
        "Django model includes sign_count as BigIntegerField (not IntegerField)",
        "passkey_user_id added to User model (CharField, PII-free)",
        "Session used for challenge storage",
        "JSON serialization pattern correctly handles py_webauthn options output (not raw string)",
        "Challenge deleted in both success and failure paths (replay protection)",
        "Registration endpoints require @login_required decorator",
        "List and delete endpoints require @login_required decorator",
        "Delete endpoint validates ownership: user can only delete own passkeys",
        "Attempting to delete another user's passkey returns 403 Forbidden",
        "Existing argon2/password login preserved",
        "v1.1.0: Challenge stored as base64-encoded string in session (request.session['passkey_challenge'] = base64.b64encode(options.challenge).decode()) — raw bytes do NOT survive Django JSON session serialization",
        "v1.1.0: transports stored as strings are converted to AuthenticatorTransport enum in excludeCredentials (e.g. AuthenticatorTransport(t) for t in pk.transports)",
        "v1.1.0: verify_authentication_response handled with try/except — no .verified attribute access (py_webauthn raises on failure)",
        "v1.1.0: passkey_user_id migration is 3-step (add nullable column → RunSQL UPDATE auth_user SET passkey_user_id = gen_random_uuid() WHERE passkey_user_id IS NULL → alter to NOT NULL UNIQUE)",
        "v1.1.0: @csrf_exempt applied to public passkey endpoints (challenge + verify) since browsers do not send CSRF tokens for these XHR calls",
        "v1.1.0: Field name is sign_count (matches py_webauthn return value), not counter — mixing causes UndefinedColumn at runtime"
      ]
    },
    {
      "id": 3,
      "prompt": "We have a Spring Boot app with Spring Security and PostgreSQL. Product wants to add 'Sign in with passkey' to the login page by next sprint.",
      "expected_output": "Backend plan using java-webauthn-server (Yubico 2.8.1), RelyingParty bean config, CredentialRepository implementation, JPA entity with all fields including aaguid, Spring @RestController with 6 endpoints using @PreAuthorize, challenge stored in HttpSession, counter update after successful authentication.",
      "should_trigger": true,
      "assertions": [
        "Plan presented before code",
        "Library: java-webauthn-server (Yubico) selected, version 2.8.1",
        "RelyingParty bean configuration mentioned",
        "RelyingParty configured with APP_ORIGIN environment variable (HTTPS origins list)",
        "RelyingParty configured with RP_ID environment variable (domain-only, no protocol or port)",
        "CredentialRepository interface implementation mentioned",
        "CredentialRepository provides lookupCredentialsByCredentialId(credentialId) method",
        "CredentialRepository does NOT rely on userId to fetch credentials (uses credentialId)",
        "JPA entity includes aaguid field (String)",
        "JPA entity includes signatureCount (long, not int)",
        "User entity gets passkeyUserId (UUID) column noted (PII-free)",
        "Spring Security preserved — not replaced",
        "6 endpoints defined with @PreAuthorize for authenticated ones (register-challenge, register-verify, auth-challenge, auth-verify, list, delete)",
        "HttpSession used for challenge storage",
        "Challenge stored with 5-10 minute TTL",
        "Challenge removed from session after verification (both success and failure)",
        "signatureCount incremented by exactly 1 AFTER verification completes",
        "signatureCount increment happens BEFORE response sent to client",
        "Challenge removal and counter update sequenced (no stale state between operations)",
        "v1.1.0: Production warning that InMemoryRegistrationStorage is non-persistent — must replace with JPA-backed CredentialRepository before going live",
        "v1.1.0: Counter update happens BEFORE returning response (no race window where two parallel requests could pass replay check)"
      ]
    },
    {
      "id": 4,
      "prompt": "I only have a Nuxt 3 frontend here. I want to add passkey sign-in. The backend is a NestJS API running separately.",
      "expected_output": "Skill stops immediately and presents the structured backend context template before generating any plan. Template covers: backend framework, database, ORM, current auth mechanism, API base URL, whether passkey endpoints already exist, and backend plan document path.",
      "should_trigger": true,
      "assertions": [
        "No plan or code generated before backend context collected",
        "A structured form or template is presented (not just a list of freeform questions)",
        "Form is presented via ask_user tool (not markdown questions)",
        "Form includes field: backend framework",
        "Form includes field: database",
        "Form includes field: ORM",
        "Form includes field: current auth mechanism",
        "Form includes field: API base URL",
        "Form includes field: whether passkey endpoints already exist (yes/no/unknown)",
        "Backend context template responses are carried into backend plan generation",
        "Backend plan is generated after context collection (not frontend plan first)",
        "Response explains that backend plan must come before frontend plan",
        "v1.1.0: Dynamic import of @simplewebauthn/browser inside onMounted() — avoids SSR errors since the package references window/document",
        "v1.1.0: useFetch / $fetch composable used for API calls (Nuxt convention) — not raw fetch()",
        "v1.1.0: navigateTo('/dashboard') or equivalent redirect after successful auth verify (Nuxt routing API, not window.location)"
      ]
    },
    {
      "id": 5,
      "prompt": "Laravel 10 app with Sanctum tokens and MySQL. We want passkeys. Also our frontend is SvelteKit in a separate repo.",
      "expected_output": "Backend plan using web-auth/webauthn-lib with Laravel controller scaffold, Eloquent migration with aaguid + passkey_user_id, Sanctum auth middleware on protected routes, JSON transports (MySQL), plus frontend plan for SvelteKit using Svelte stores.",
      "should_trigger": true,
      "assertions": [
        "Plan presented before code",
        "Library: web-auth/webauthn-lib for Laravel",
        "Eloquent migration uses JSON for transports storage (MySQL does not support native arrays)",
        "Migration follows Laravel naming convention: timestamp_create_passkeys_table.php",
        "Migration includes up() and down() methods with correct Schema syntax",
        "Sanctum middleware applied to protected routes (register, list, delete endpoints)",
        "passkey_user_id column added to users table migration (PII-free identifier)",
        "aaguid column in passkeys migration (String, stores provider identifier)",
        "SvelteKit frontend uses Svelte reactive assignment pattern: let credentials = []",
        "SvelteKit example with autocomplete='username webauthn' on input element",
        "Separate repos acknowledged in plan",
        "Frontend plan explicitly notes: 'Wait for backend endpoint confirmation before deployment'",
        "Separate repos acknowledged — frontend plan waits for backend endpoint confirmation",
        "v1.1.0: sodium PHP extension required for EdDSA (Ed25519) passkey validation — must be enabled in php.ini",
        "v1.1.0: Production warning if sodium is not enabled — error message guidance ('extension=sodium' in php.ini, then restart php-fpm)"
      ]
    },
    {
      "id": 6,
      "prompt": "Add passkeys to our Go API using Gin and our React frontend",
      "expected_output": "Backend plan using go-webauthn/webauthn with Gin handlers, full handler setup (RegisterChallenge, RegisterVerify, AuthChallenge, AuthVerify, ListPasskeys, DeletePasskey), WebAuthnUser interface implementation, FinishDiscoverableLogin for authentication, counter update, Go struct with AAGUID field. React hook pattern for frontend with conditional UI.",
      "should_trigger": true,
      "assertions": [
        "Plan presented before code",
        "Library: go-webauthn/webauthn selected",
        "WebAuthnUser interface implementation mentioned (required by library)",
        "WebAuthnUser interface includes: WebAuthnID(), WebAuthnName(), WebAuthnDisplayName(), WebAuthnIcon(), WebAuthnCredentials() methods",
        "All 6 handler functions listed (RegisterChallenge, RegisterVerify, AuthChallenge, AuthVerify, ListPasskeys, DeletePasskey)",
        "FinishDiscoverableLogin or equivalent discoverable credential flow mentioned",
        "FinishDiscoverableLogin used ONLY in auth flow (not in registration)",
        "Non-discoverable credentials still work: allowCredentials populated in auth handler",
        "Counter validation: verify newCounter > oldCounter (replay protection mentioned)",
        "Counter update after successful authentication mentioned",
        "Go struct includes AAGUID field (string, provider identifier)",
        "Ownership check on delete (filter by userID, can only delete own passkeys)",
        "Delete endpoint returns 403 if user tries to delete another user's passkey",
        "Challenge store with TTL mentioned (reasonable expiry, 5-10 minutes)",
        "Failed counter validation returns 400 Bad Request with clear error message",
        "Challenge deleted even if counter validation fails (replay protection in catch block)",
        "React usePasskey hook pattern provided",
        "React usePasskey hook exposes: register(), authenticate(), listPasskeys() methods",
        "React usePasskey hook state includes: {loading, error, passkeys}",
        "Conditional UI with useBrowserAutofill in React startAuthentication() call",
        "startAuthentication({ useBrowserAutofill: true }) in authentication flow",
        "Cross-device authenticatorAttachment check included (optional platform vs. cross-device)",
        "v1.1.0: React: separate autofillPending and loading states (not single 'loading') — autofillPending tracks the silent conditional UI request, loading tracks explicit button click",
        "v1.1.0: React: AbortController ref aborts the conditional UI request when the explicit 'Sign in with passkey' button is clicked — prevents UI lockup",
        "v1.1.0: React: passkey logic extracted into a usePasskey custom hook (not inlined in component)"
      ]
    },
    {
      "id": 7,
      "prompt": "I want to understand the security implications of adding passkeys. What are the risks and how do they compare to passwords?",
      "expected_output": "This should NOT trigger the passkeys-auth-migration skill — it is a general security question, not a migration request. The skill should not activate.",
      "assertions": [
        "Skill does not activate for a general information question",
        "No migration plan is generated"
      ],
      "should_trigger": false
    },
    {
      "id": 8,
      "prompt": "How do I set up OAuth2 with Google in my Next.js app?",
      "expected_output": "This should NOT trigger the passkeys-auth-migration skill — it is an OAuth/SSO question, not a passkey migration.",
      "assertions": ["Skill does not activate for OAuth questions"],
      "should_trigger": false
    },
    {
      "id": 9,
      "prompt": "We implemented passkeys last week on our Express/React app but the passkey autofill never shows up in the username field. Users registered passkeys but they never appear in Chrome's autofill dropdown.",
      "expected_output": "Skill triggers and directs agent to references/troubleshooting.md. Agent diagnoses the Conditional UI problem: checks for autocomplete='username webauthn' on input, HTTPS requirement, useBrowserAutofill:true in startAuthentication(), mediation:'conditional' flag, empty allowCredentials array, and browser version support.",
      "assertions": [
        "Skill triggers for a passkey debugging scenario",
        "references/troubleshooting.md is referenced or loaded",
        "Diagnostic sequence specified: 1. HTTPS check, 2. autocomplete attribute, 3. useBrowserAutofill flag, 4. mediation:'conditional', 5. allowCredentials validation, 6. Browser version",
        "autocomplete='username webauthn' check mentioned (on username/email input, NOT password)",
        "HTTPS requirement mentioned (localhost allowed for testing)",
        "useBrowserAutofill:true in startAuthentication() mentioned",
        "mediation:'conditional' or useBrowserAutofill flag mentioned",
        "allowCredentials: [] (empty array required for discoverable flow) mentioned",
        "Browser support table or version requirements mentioned (Chrome 108+, Edge 108+, Safari 16+)",
        "Private browsing mode limitation mentioned as potential cause (credentials not persisted across sessions)",
        "Distinction provided: no dropdown vs. dropdown with no passkeys listed (different root causes)",
        "For each identified issue, a specific code fix provided with before/after examples"
      ],
      "should_trigger": true
    },
    {
      "id": 10,
      "prompt": "I built passkey auth for our FastAPI + React app. How should I write tests for the passkey endpoints? Our CI doesn't have hardware authenticators.",
      "expected_output": "Skill triggers and directs agent to references/testing-guide.md. Agent provides a testing strategy covering unit tests that mock the webauthn library (challenge deletion in catch block, counter validation), integration tests for endpoint ownership checks, E2E tests using Chrome virtual authenticator via CDP (Playwright/Puppeteer), and CI/CD setup with HTTPS self-signed cert.",
      "assertions": [
        "Skill triggers for a passkey testing question",
        "references/testing-guide.md is referenced or loaded",
        "Unit test approach: mock @simplewebauthn/server (or equivalent library, not HTTP layer)",
        "Unit test scope covers: challenge generation, credential creation verification, counter validation",
        "Challenge deletion test explicitly asserts: delete() called in both success and catch blocks",
        "Counter validation test mentioned",
        "Integration tests for endpoint ownership checks (can user delete another's passkey?)",
        "E2E testing via virtual authenticator (Chrome DevTools Protocol) mentioned",
        "Playwright recommended for cross-browser virtual authenticator support",
        "Puppeteer mentioned for Chrome-only scenarios (simpler setup, less cross-browser testing)",
        "Playwright or Puppeteer CDP session for addVirtualAuthenticator support",
        "Self-signed certificate setup mentioned: openssl req -x509 -nodes -days 365",
        "APP_ORIGIN='https://localhost:3000' (matching cert domain) mentioned",
        "RP_ID='localhost' (NOT '127.0.0.1' — breaks virtual authenticator)",
        "Virtual authenticator tests use retry logic: max 3 attempts, 1s between retries",
        "Timeout set to 10-15 seconds per test (virtual authenticator slower than hardware)",
        "Test verifies: attempting auth with deleted challenge returns 'Challenge not found' error"
      ],
      "should_trigger": true
    },
    {
      "id": 11,
      "prompt": "Add passkeys to my Next.js 15 app. We use next-intl for translations and have locale files in src/locales/ (en.json, fr.json, es.json). I have a NestJS backend already wired up with the basic /auth endpoints. Don't break the existing i18n setup — every user-visible string in the app goes through the t() function.",
      "expected_output": "Skill triggers, Phase 0 detects next-intl + locale files (sets i18n_detected = true), Phase 2 plan includes Step 9 — Internationalization with the passkeys.* key catalog, and generated component code uses t('passkeys.createButton') etc. instead of hardcoded English strings. references/i18n-guide.md is loaded.",
      "assertions": [
        "Plan presented before code",
        "Phase 0 detects next-intl in dependencies and sets i18n_detected = true",
        "Phase 0 detects locale files under src/locales/ (en.json, fr.json, es.json)",
        "references/i18n-guide.md is loaded or referenced",
        "Phase 2 plan includes Step 9 — Internationalization",
        "Generated component code uses t('passkeys.<key>') — NOT hardcoded English strings",
        "Key namespace passkeys.* is used consistently across all generated code",
        "Required keys catalog presented covering at minimum: passkeys.createButton, passkeys.signInButton, passkeys.heroHeadline, passkeys.heroBenefit, passkeys.cardSyncedLabel, passkeys.cardDeviceOnlyLabel, passkeys.deleteConfirm, passkeys.renameLabel, passkeys.errors.*",
        "Locale JSON additions shown for at least en.json (other locales noted as needing translation)",
        "Error messages (NotAllowedError, InvalidStateError, etc.) also use t() — not hardcoded",
        "Error keys nested under passkeys.errors.* (not flat at top level)",
        "Plan notes: passkeys.* namespace is kept separate from app's existing keys"
      ],
      "should_trigger": true
    },
    {
      "id": 12,
      "prompt": "My passkey implementation works but users complain they can't tell their passkeys apart. They have a MacBook and an iPhone — both passkeys show up as 'iCloud Keychain' in their account settings. Add the ability to rename existing passkeys, and use a sensible default name on creation. Stack is NestJS + Prisma + React with MUI.",
      "expected_output": "Skill triggers. Plan adds PATCH /auth/passkey/:id endpoint with auth + ownership check + name validation. Frontend gets inline rename UI (pencil icon → input → Save/Cancel). Default name is resolved from AAGUID at registration time (e.g. 'iCloud Keychain', 'Google Password Manager', falling back to 'Passkey'). FIDO MDS referenced for AAGUID-to-provider mapping. Rename endpoint added to security checklist and completeness checklist.",
      "assertions": [
        "Plan presented before code",
        "PATCH /auth/passkey/:id endpoint added to the backend plan",
        "Endpoint requires auth guard (e.g. @UseGuards(JwtAuthGuard))",
        "Ownership check enforced: filter by both id AND userId before update — not just id",
        "Server-side name validation: non-empty, max 100 chars, trimmed",
        "Name field is sanitized / escaped before storage (or ORM handles it explicitly)",
        "Default passkey name resolved from AAGUID at registration time (e.g. 'iCloud Keychain', 'Google Password Manager')",
        "AAGUID-to-name fallback: 'Passkey' when AAGUID is unrecognized",
        "FIDO MDS (Metadata Service) referenced for AAGUID-to-provider mapping",
        "Frontend: inline rename UI pattern — pencil/edit icon → text input → Save/Cancel buttons",
        "MUI Button + TextField + IconButton used (matches the project's existing component library)",
        "Rename endpoint included in the Implementation Completeness Checklist",
        "Security checklist mentions: rename endpoint must verify ownership before update",
        "Rename endpoint is rate-limited (same as list/delete endpoints)",
        "v1.2.0: Notes the nickname rename is RP-side only — if the account's username/displayName changes server-side, PublicKeyCredential.signalCurrentUserDetails is the mechanism to sync the credential manager's copy"
      ],
      "should_trigger": true
    },
    {
      "id": 13,
      "prompt": "My passkey auth verify endpoint returns 401 every time. The browser ceremony completes successfully (no client-side error), but my backend can't find the user matching the credential. I'm on NestJS + Prisma + Postgres with @simplewebauthn/server. The credential was registered fine and exists in the passkeys table.",
      "expected_output": "Skill triggers (troubleshooting context recognized). references/troubleshooting.md loaded. Diagnoses the userHandle base64url encoding issue: SimpleWebAuthn returns userHandle from the authentication response as a base64url-encoded string, which must be decoded before being used as a DB lookup key (the stored passkey_user_id is the raw UUID/string). Provides the fix: Buffer.from(userHandle, 'base64url').toString() before Prisma lookup.",
      "assertions": [
        "Skill triggers for the passkey debugging scenario",
        "references/troubleshooting.md is referenced or loaded",
        "Diagnoses: userHandle from authentication response is base64url-encoded — must be decoded before DB lookup",
        "Fix code shown: Buffer.from(userHandle, 'base64url').toString() (or equivalent atob-like decode) before Prisma findUnique",
        "Mentions userHandle in the auth response is the stored passkey_user_id, NOT user.id",
        "Distinguishes from rpID mismatch (rpID gives a different symptom — verification rejects on the server side, not user lookup)",
        "Distinguishes from credential lookup by credentialId (mentions both lookup paths exist)",
        "Notes that sometimes the lookup should go via credentialId first, with userHandle as a fallback for discoverable credentials"
      ],
      "should_trigger": true
    },
    {
      "id": 14,
      "prompt": "Our users all sign in with passwords. We shipped passkey support last quarter but adoption is stuck under 5%, and product vetoed any more popups or nag banners. How do we get password users onto passkeys automatically? Stack: React + NestJS + SimpleWebAuthn.",
      "expected_output": "Skill triggers as an adoption/enhancement request on an existing passkey implementation — no full migration plan regenerated. Recommends conditional create (automatic passkey upgrade): immediately after a successful password sign-in, feature-detect PublicKeyCredential.getClientCapabilities().conditionalCreate and call startRegistration({ optionsJSON, useAutoRegister: true }) — which sets mediation: 'conditional' on navigator.credentials.create(). Explains support (Safari 18+, Chrome 136+ desktop / 142+ Android), Chrome constraints (short window after password login, password must be saved in the credential manager, Google Password Manager makes the final call), silent-failure semantics, excludeCredentials for duplicate prevention, passive post-success notification, and a trigger policy that avoids firing on every login.",
      "should_trigger": true,
      "assertions": [
        "Skill triggers for a passkey adoption/upgrade request without regenerating a full migration plan",
        "v1.2.0: Conditional create recommended: navigator.credentials.create({ mediation: 'conditional' }) — or SimpleWebAuthn v13 startRegistration({ useAutoRegister: true }) — attempted immediately after successful password sign-in",
        "v1.2.0: Feature detection via PublicKeyCredential.getClientCapabilities() checking conditionalCreate === true before attempting",
        "v1.2.0: Support constraints stated: Safari 18+, Chrome 136+ desktop / 142+ Android; silent no-op elsewhere",
        "v1.2.0: Chrome-specific constraints mentioned: works only within a few minutes of the password sign-in, the password must be saved in the credential manager, and Google Password Manager makes the final creation decision",
        "v1.2.0: excludeCredentials still populated in the conditional-create registration options (duplicate prevention)",
        "v1.2.0: No interstitial before the upgrade; user notified passively after success (toast/email); InvalidStateError and silent failure caught without any user-facing error state",
        "v1.2.0: A trigger policy is proposed that does NOT fire on every login (e.g. skip users who already have a passkey, attempt cap or cooldown)"
      ]
    },
    {
      "id": 15,
      "prompt": "Make our sign-in button smart: when the user clicks 'Sign in', if they have a passkey on this device it should go straight to the passkey prompt, otherwise show the normal login form. Most of our traffic is Chrome. Vue 3 + Express.",
      "expected_output": "Skill triggers as a UX enhancement on an existing flow. Recommends WebAuthn immediate UI mode: navigator.credentials.get({ publicKey, uiMode: 'immediate' }) (Chrome 149+ stable). Warns that the earlier origin-trial syntax mediation: 'immediate' no longer triggers the feature. NotAllowedError when no local credentials exist is handled as a silent fallback to the regular form or conditional UI. Chrome-only as of mid-2026, so it is shipped as progressive enhancement. Notes immediate mode surfaces only locally-available credentials (suppresses cross-device/hybrid).",
      "should_trigger": true,
      "assertions": [
        "Skill triggers for the smart sign-in button request",
        "v1.2.0: uiMode: 'immediate' in navigator.credentials.get() presented as the mechanism (Chrome 149+ stable)",
        "v1.2.0: Warns that the earlier origin-trial syntax mediation: 'immediate' no longer triggers immediate mode",
        "v1.2.0: NotAllowedError (no local credentials) handled as silent fallback to the regular form / conditional UI — never a user-facing error",
        "v1.2.0: Chrome-only as of mid-2026 — progressive enhancement; other browsers get the standard flow",
        "v1.2.0: Notes immediate mode suppresses cross-device/hybrid options (locally-available credentials only)"
      ]
    },
    {
      "id": 16,
      "prompt": "We migrated all usernames server-side (rebrand: first.last → employee IDs) and users' passkey pickers still show the old usernames. How do we fix the stale names in their password managers? Does our existing passkey rename feature cover this?",
      "expected_output": "Skill triggers (passkey management). Explains the rename endpoint (RP-side nickname) does NOT update the credential manager; the fix is PublicKeyCredential.signalCurrentUserDetails({ rpId, userId, name, displayName }) called in the browser on the RP origin (e.g. after the user's next sign-in), with userId base64url-encoded to match the stored userHandle. Support: Chrome 132+, Safari 26 (known WebKit bug — fire-and-forget inside try/catch, never await-block the UI).",
      "should_trigger": true,
      "assertions": [
        "Skill triggers for the passkey management scenario",
        "v1.2.0: PublicKeyCredential.signalCurrentUserDetails({ rpId, userId, name, displayName }) recommended for syncing username/displayName changes to the passkey provider",
        "v1.2.0: userId passed base64url-encoded to match the stored userHandle",
        "v1.2.0: Distinction drawn: renaming a passkey nickname (PATCH endpoint) is RP-side only; user.name/displayName changes require the Signal API",
        "v1.2.0: Support stated: Chrome 132+, Safari 26 — with WebKit bug caveat (promise may never resolve): wrap in try/catch, never await-block the UI on it",
        "v1.2.0: Clarifies it is a browser-side call made on the RP origin (e.g. on the user's next visit/sign-in), not a server API"
      ]
    },
    {
      "id": 17,
      "prompt": "We run example.co.uk and example-app.com and want one passkey to work on both domains. Does Firefox support this now? Show me the exact file I need to serve.",
      "expected_output": "Skill triggers (multi-domain passkey configuration). Explains Related Origin Requests: serve https://example.co.uk/.well-known/webauthn with Content-Type: application/json containing {\"origins\": [\"https://example.co.uk\", \"https://example-app.com\"]}, then use rpID example.co.uk from both origins. Support: Chrome/Edge 128+, Safari 18+, and Firefox 152+ (May 2026 — the last major browser gap, now closed). Distinguishes /.well-known/webauthn from /.well-known/passkey-endpoints. Mentions the label limit (~5 distinct eTLD+1 labels).",
      "should_trigger": true,
      "assertions": [
        "Skill triggers for the multi-domain passkey configuration question",
        "v1.2.0: Concrete /.well-known/webauthn JSON shown: {\"origins\": [\"https://example.co.uk\", \"https://example-app.com\"]}",
        "v1.2.0: Serving requirements stated: Content-Type: application/json, hosted at the rpID domain root, ~5-label limit mentioned",
        "v1.2.0: Firefox question answered correctly: supported since Firefox 152 (May 2026) — NOT claimed as unsupported or 'no timeline'",
        "v1.2.0: Chrome/Edge 128+ and Safari 18+ support also stated",
        "v1.2.0: Distinguished from /.well-known/passkey-endpoints (Google Password Manager upgrade prompts)"
      ]
    },
    {
      "id": 18,
      "prompt": "We want end-to-end encrypted notes in our app, and we'd like the user's passkey to unlock the encryption key so there's no extra password. Is that possible with WebAuthn?",
      "expected_output": "Skill triggers (advanced passkey feature). Recommends the WebAuthn PRF extension: request extensions: { prf: { eval: { first: <salt> } } } during authentication (navigator.credentials.get()), derive a symmetric key client-side (e.g. HKDF) from the PRF output, never send the PRF output to the server. Caveats: PRF results are only returned during authentication ceremonies; availability varies per credential/provider — test each credential and keep a fallback unlock path. Support: Chrome/Edge, iOS 18+/macOS 15+, Windows Hello (since Feb 2026), Google Password Manager. references/advanced-features.md is the source.",
      "should_trigger": true,
      "assertions": [
        "Skill triggers for the passkey-based encryption request",
        "v1.2.0: PRF extension identified as the mechanism (extensions: { prf: ... }); references/advanced-features.md loaded or referenced",
        "v1.2.0: PRF output obtained during authentication — navigator.credentials.get() — not treated as available from registration alone",
        "v1.2.0: Per-credential availability caveat: PRF support varies by provider/credential — test each credential, keep a fallback unlock path",
        "v1.2.0: Support summary includes Chrome/Edge, iOS 18+/macOS 15+, Windows Hello (since Feb 2026), Google Password Manager",
        "v1.2.0: PRF output treated as client-side key material (e.g. HKDF-derived), never sent to the server"
      ]
    },
    {
      "id": 19,
      "prompt": "Our staff all use YubiKeys. When they register a passkey the browser keeps pushing them toward phones and QR codes. How do we steer the flow to security keys? Spring Boot backend, plain JS frontend.",
      "should_trigger": true,
      "expected_output": "Skill triggers (registration UX steering). Recommends WebAuthn hints: hints: ['security-key'] in the registration (and authentication) options (Chrome 128+), values security-key | client-device | hybrid. Hints express a soft preference that steers browser UI without hard-blocking other authenticators — contrasted with authenticatorAttachment which filters. Backend guidance fits the Spring Boot stack (hints included in the options JSON the backend serves).",
      "assertions": [
        "Skill triggers for the authenticator-steering request",
        "v1.2.0: hints: ['security-key'] on the options presented as the steering mechanism (Chrome 128+)",
        "v1.2.0: The three hint values security-key | client-device | hybrid mentioned",
        "v1.2.0: Hints positioned as a soft preference that does not hard-block other authenticators, contrasted with authenticatorAttachment",
        "v1.2.0: Backend guidance fits the stack: hints delivered via the registration options JSON served by the Spring Boot backend"
      ]
    }
  ]
}
